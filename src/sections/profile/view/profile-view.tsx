'use client';
import { Box } from '@mui/material';

const ProfileView = () => {

  return (
    <Box
      sx={{
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        justifyContent: 'center',
        height: '100%',
        gap: 2,
      }}
    >
      Profile
    </Box>
  );
};

export default ProfileView;
